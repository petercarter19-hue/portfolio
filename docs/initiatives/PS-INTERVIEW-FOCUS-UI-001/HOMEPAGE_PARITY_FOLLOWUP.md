# PS-HOME-INTERVIEW-FOCUS-PARITY-001

## Status

**Activated downstream homepage-parity package — visual authority required
before runtime work.**

This activation is the exact downstream dependency required by the
cross-product homepage projection contract for the material
`PS-INTERVIEW-FOCUS-UI-001` redesign. Homepage parity is **open**. This record
does not modify the homepage, assign a runtime writer, or claim that the
current projection already matches the refreshed Studio.

## Affected projection and canonical upstream

- Affected public surface: the logged-out `/` Interview Studio scene and its
  illustrative walkthrough.
- Canonical product route: `/interview-studio`.
- Upstream authority before deployment: the accepted
  `PS-INTERVIEW-FOCUS-UI-001` V3/V2 implementation candidate and Pete's
  compact-height correction.
- Upstream authority after deployment: the exact accepted, merged, deployed,
  and live `/interview-studio` release, with its source, merge, pipeline,
  production release identity, and live evidence recorded in the parent
  package's completion report.

The existing homepage scene continues to link to the real public Studio and
retains its fictional, illustrative, no-input, no-request, and no-storage
truth boundary. It was accepted and released by the historical
`PS-HOME-INTERVIEW-PARITY-001` package for the former 5A/5C Studio. That
historical package remains complete and preserved, including all Claude
architecture/implementation attribution and exact SHAs. It is not reopened or
rewritten by this activation.

The new Studio materially changes visual authority, theme, hierarchy,
responsive composition, and professional finish. The older projection is
therefore a truthful link and illustration but a stale product projection.
It may not be reported as current visual parity.

## Required outcome

Create a focused, visitor-facing projection of the accepted and live Focus
Studio that:

- remains recognizably the same product without copying an application screen;
- presents written Interview Me practice as the primary journey;
- maps its fixed illustrative states to the real product's current question,
  answer, processing, coaching, and improvement hierarchy;
- carries the current white/navy/cobalt/teal light language and compatible
  dark treatment;
- preserves truthful public/browser-local, AI-proposal, local-media, and
  no-side-effect labels;
- links to the canonical `/interview-studio` route;
- remains deterministic and performs no visitor input, AI request, media
  request, or browser storage; and
- passes separate desktop/mobile, responsive, accessibility, truth, visual,
  Azure release, and live-production acceptance.

## Visual-creation and inventory gate

The homepage projection is a separate material production-intent
composition. Before runtime work:

1. complete the controlled
   `docs/templates/PAGE_PURPOSE_AND_NON_REDUNDANCY_INVENTORY.md` for the
   homepage scene;
2. ChatGPT creates the exact desktop/mobile and required state authority,
   using ChatGPT image generation for any new or materially revised bitmap;
3. Pete selects and locks the exact durable authority; and
4. the designated manager records the authority path, hashes, frames/states,
   intended viewports, truth review, and acceptance status.

The live Focus Studio is upstream product authority. The existing homepage
implementation and the historical parity package may inform preserved
interaction and truth behavior, but neither may substitute for the new
ChatGPT-created, Pete-locked projection authority.

## Ownership and sequencing

- Product owner and final visual authority: Pete.
- Visual-creation lane: ChatGPT.
- Activation and handoff manager: the current ChatGPT Work/Codex task for
  `PS-INTERVIEW-FOCUS-UI-001`.
- Runtime writer: **unassigned**.
- Runtime branch: none.
- Homepage file reservation: none at this activation gate.

The real Studio releases first because it is the upstream authority and the
current implementation package does not own homepage files. This downstream
package is sequenced immediately after that release. Runtime implementation
must use a fresh branch from then-current Azure `origin/main`, one designated
manager, one active writer, exact reserved homepage files, and the newly
locked homepage authority.

No current Interview Studio runtime file, shared shell, route, endpoint,
request/prompt/rubric, authentication boundary, storage key, database,
deployment definition, or another lane's work is transferred to this
downstream package.

## Required comparison and closeout evidence

The downstream completion report must separately provide:

- accepted live Studio and homepage scene pairs at desktop and mobile;
- purpose, dominant action, terminology, truth-label, theme, hierarchy,
  responsive-model, canonical-link, and professional-finish comparison;
- the required continuous compare-refine record or Pete-run correction record;
- keyboard, focus, 200-percent reflow, reduced-motion, long-content,
  no-JavaScript, and truthful failure/fallback evidence;
- proof that the walkthrough makes no input, AI/network, media, or storage
  request;
- explicit Pete and designated-manager visual/product acceptance;
- exact branch/source/merge/deployed SHAs, Azure PR and pipeline, production
  release identity, and live desktop/mobile verification; and
- an update that closes homepage parity only after the accepted projection is
  live.

Until all of that evidence passes, `PS-HOME-INTERVIEW-FOCUS-PARITY-001`
remains open and the parent Studio release must report the homepage projection
as **Downstream Package Required**, not Current.

# Repository Discovery and Preservation Map

Date: 2026-07-28

## Authority and workspace

- Fresh Azure fetch/prune completed before activation.
- Authoritative base: `origin/main` at
  `a85ffbc93a1def86f99db66df26702a59aff4cbc`.
- Clean worktree:
  `C:\Users\peter\Documents\portfolio-interview-focus-ui-001`.
- Task branch: `work/2026-07-28-ps-interview-focus-ui-001`.
- The dirty primary checkout and all unrelated worktrees remain untouched.

## Single-writer result

No active writer owns the Interview template, scoped CSS, scoped JavaScript, or
focused test file. This task is the sole writer for those four files.

`app.py` overlaps an active PS-OPS lane and a separate Claude branch, and is outside
the V3 frontend contract. It is read-only for this initiative. The existing Claude
provider/model strings and all backend behavior remain unchanged.

## Existing behavior reused

- Interview Me: current question/session state, review, improve, retry, continue,
  local draft/history, and API contracts.
- Interview AI: current model-answer request, answer-basis selection, grounding,
  failure, and transfer seams.
- Video Practice: current media permission, preview, recording, playback, cleanup,
  transcript, and text-coaching seams.
- History: current browser-local storage prefix, session/goal records, deep link,
  and storage truth.
- Shared dictation: the existing recognition pipeline writes into the same textarea.

No second business state machine, persistence store, route, request schema, or media
transport is authorized or needed.

## Claude provenance preserved

The current repository and remotes include historical or active Claude-named
branches and review/routing records. None is a source branch for this UI change, and
none will be renamed, deleted, rewritten, merged, or cherry-picked. Exact existing
references remain independently recoverable. This initiative adds no claim that
Claude work was Codex work.

## Known intentional authority adaptations

- Package wording such as “Proof you may have missed” is illustrative and conflicts
  with current site truth language. Production uses “Relevant history you may have
  missed.”
- V3 remains primary over V2 when their pixels differ.
- Pete's compact-height correction supersedes both for composer geometry.
- Written V3 AI/Video states are presentation variants of the pictured shells, not
  new product architecture.

## Homepage parity

This package does not own the homepage. The historical
`PS-HOME-INTERVIEW-PARITY-001` package is complete and remains the preserved
release record for convergence with the former 5A/5C Studio; it is not reopened,
renamed, or rewritten.

Because this Focus redesign materially changes the Studio's visual authority,
theme, hierarchy, responsive composition, and professional finish, it triggers a
new homepage-impact assessment. The exact downstream package
`PS-HOME-INTERVIEW-FOCUS-PARITY-001` is activated in
`HOMEPAGE_PARITY_FOLLOWUP.md`. The accepted and live refreshed Studio becomes
that package's upstream authority. Homepage parity remains open until the new
ChatGPT-created, Pete-locked projection is separately implemented, accepted,
released, and verified live. No homepage file is modified here.

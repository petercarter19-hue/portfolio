# Sol Ultra handoff — architecture and primary Feed checkpoint

## Owner instruction

Pete approved the 2026-08-01 Voice-first Community authority and requested a
new Sol Ultra session. That session may reconcile the technical architecture
and implement the primary Community Feed page, then must stop and return the
real browser page for Pete's inspection before building additional states.

## Repository state

- Authoritative remote/base: Azure DevOps `origin/main` at
  `2494aa73ed95bfbe97d8cf42f712b9929759e0b2`.
- Working branch: `codex/2026-07-31-community-public-pilot-runtime`.
- Existing task worktree:
  `/Users/petercarter/portfolio-community-runtime-20260731`.
- The branch contains an uncommitted protected implementation candidate. Do
  not reset, clean, replace, or discard it. Preserve unrelated work.
- Exact visual authority:
  `docs/initiatives/PS-COMMUNITY-FEED-VISUAL-001/visual-authority/2026-08-01-pete-voice-first-lock/`.

## Required first responsibility — architecture reconciliation

Read `START_HERE.md`, `CURRENT_BASELINE.yaml`, the current Constitution and
Roadmap, `AI_WORKFLOW.md`, `AI_MODEL_AND_ROLE_ROUTING.md`,
`PEERSLATE_SITE_RULES.md`, `OWNER_VISUAL_INTEGRITY_STANDARD.md`, this package,
the exact authority manifest, and the existing `PS-VOICE-001` protected
contracts/services.

Inspect the current Community candidate rather than re-authoring it. Produce a
concise architecture amendment that maps the locked UI to existing Feed,
publication, attachment, and Voice components; defines only missing protected
Community-to-Voice seams; and preserves server-derived identity, private local
drafts, explicit Public selection/confirmation, transient private dictation,
reviewed transcript insertion, and separate Publish/Reply.

Do not originate a competing visual design. Do not broaden media types,
audiences, messaging, member authoring, schema, navigation, or deployment.

## Required second responsibility — one primary page only

After recording the architecture amendment, implement the default primary
Community Feed page at `/the-slate` against the locked authority:

- desktop primary Feed state, with responsive behavior sufficient to avoid
  knowingly blocking the later mobile pass;
- approved compact typography and restrained Workshop-derived color language;
- large Feed media, compact icon-led actions, no visible Save or Open post;
- truthful Pete-only fixture/local preview state; and
- the primary Feed card's default/closed interaction presentation.

Use the existing local protected candidate and fixture preview. Do not yet
complete or fan out the full-post modal, reply branches, composer state matrix,
Voice state matrix, failure/permission states, SQL migration, production Blob/
Speech integration, Break integration, retention job, Candidate, PR, merge, or
deployment.

## Hard stop

Stop immediately after the primary Feed page is implemented and rendered in a
real local browser. Return:

1. the local preview URL;
2. one desktop screenshot at the comparable locked viewport;
3. changed files;
4. focused test/browser results;
5. a short visual mismatch list; and
6. an explicit statement that secondary states and release work have not begun.

Wait for Pete's review and new instruction. Do not continue automatically.

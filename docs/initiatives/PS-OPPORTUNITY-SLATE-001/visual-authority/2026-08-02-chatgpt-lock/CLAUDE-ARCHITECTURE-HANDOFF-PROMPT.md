# Claude assignment

The Opportunity Slate visual-creation round is complete.

Do not generate or edit another image. Create the final architecture-ready visual-authority and implementation handoff from this folder.

## Primary flow

1. Role - Bring a role
2. Review - Source
3. Review - Requirements
4. Alignment - Results not saved
5. Alignment - Saved privately

## Supporting states

1. Role - Voice input active
2. Role - Source processing
3. Review Requirements - Analysis processing
4. Supporting-state sheet covering public-link import unavailable, analysis failure, changed inputs requiring reanalysis, and delete failure

## Authority rules

- Image 4 controls shared Alignment geometry, separate-card structure, uniform 12-pixel vertical spacing, shadows, typography hierarchy, and palette.
- Image 5 controls only saved-state content and actions.
- Saved and unsaved Alignment use the same implementation component and geometry.
- Saving does not move, flatten, close, or regenerate the evidence workspace.
- Required qualifications, Preferred qualifications, Responsibilities, and Informational statements remain separate cards.
- Near-black navy is used for headings and primary text. Muted slate is used for supporting copy. Cobalt is reserved primarily for actions, links, selected outlines, and important icons.
- Opportunity Slate is separate from Workshop.
- Ask Slate AI remains in the Opportunity Slate subheader.
- Voice and text edit the same response. Voice never automatically submits, confirms, analyzes, saves, publishes, or navigates.
- Required and Preferred qualification accounting remains visible.
- Do not introduce an overall score, percentage, application recommendation, employer prediction, or traffic-light verdict.
- AI proposes; the member confirms.
- Nothing saves, publishes, shares, deletes, or reanalyzes without explicit member action.

## Processing and lifecycle rules

- Processing stays inside the existing workbench and uses bounded descriptive stages.
- During requirements analysis, correction controls are read-only. Cancel analysis restores editing.
- Processing never exposes invented partial results.
- Saved privately and analytical currency are separate truths.
- Input changes require explicit reanalysis.
- Previous saved results remain identifiable and are never silently overwritten.
- New analysis results remain unsaved until explicitly saved.
- Failed deletion leaves the slate visibly saved.
- Failed import or analysis preserves confirmed inputs and provides safe retry or fallback actions.

## Deliverable

Produce one concise architecture handoff containing:

1. Final member flow and state machine.
2. Shared shell versus state-specific content.
3. Component inventory and ownership.
4. Layout, spacing, typography, palette, and shadow rules.
5. Voice-first interaction contract.
6. Processing, failure, lifecycle, and reanalysis behavior.
7. Responsive behavior for desktop, 200 percent zoom, and 320-pixel reflow.
8. Accessibility requirements.
9. Explicit mismatch register identifying generated-image details that implementation must correct.
10. Implementation acceptance checklist mapped to the supplied images.

Do not write runtime code, create another visual, add product scope, or reinterpret the approved flow. Ask Pete no more than three questions, and only if a decision is genuinely required before implementation.


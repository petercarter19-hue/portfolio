# ChatGPT visual creation, scrutiny, and bounded-package kickoff

**Initiative:** PS-SLATE-STUDIO-IA-001
**Owner:** Pete
**Working ecosystem:** ChatGPT Work/Codex
**Current gate:** create and scrutinize production-intent visual concepts from
the two Pete-approved page-purpose inventories.
**Runtime authority:** none. This session creates visual authority and prepares
bounded implementation-information packages; it does not implement, merge, or
enable Workshop or Build Your Future.

## 1. Start from current truth

Before work, follow `START_HERE.md`, fetch/prune Azure `origin`, and create a
fresh isolated branch/worktree from exact current `origin/main`. Read:

1. `docs/governance/CURRENT_BASELINE.yaml`;
2. `docs/governance/CURRENT_STATE.md`;
3. `docs/governance/ACTIVE_INITIATIVES.md`;
4. the controlling Markdown Bible and Roadmap named by the baseline;
5. `docs/governance/OWNER_VISUAL_INTEGRITY_STANDARD.md`;
6. `docs/initiatives/PS-SLATE-STUDIO-IA-001/12_OWNER_CORRECTION_RECORD_GOALS_WORKSHOP_AND_PROJECTS.md`;
7. the Pete-approved Workshop inventory in document 13; and
8. the Pete-approved Build Your Future inventory in document 14.

The rejected branch
`codex/2026-07-24-slate-studio-slice-2-architecture` at
`f6c2b52763d50d0773f20294acacd8d8165e59da` is historical evidence only:
preserve it, do not merge it, and do not reuse its work-history model, schema,
language, or visuals.

## 2. Parallel-lane boundary

Claude is still working on a separately assigned task. Treat that lane as
active and protected. Do not inspect, edit, merge, supersede, or infer
completion of Claude's files or branch. Wait for Pete to provide Claude's exact
branch and pushed SHA before any later reconciliation. In particular, do not
change public Interview Studio, Interview Me audio, Voice behavior, or adjacent
shared runtime files during this visual-definition session.

## 3. Visual creation deliverables

ChatGPT is the sole creator of every new or materially revised production-intent
visual in this round.

### Workshop

Create multiple purposefully different concepts, not cosmetic color swaps. Each
must remain one same-page Workshop with:

- all four persistent doors available at every useful point;
- one dominant Workbench;
- same-page pop-outs, drawers, or contextual expansion where practical;
- Voice, optional Ask Slate, governed evidence, and private notes;
- content proposals that can later update the résumé/profile page without
  becoming a résumé-template builder;
- separate, reviewable Story, Interview, and Goal proposals;
- a restrained Worth Trying area; and
- an explicit product reason for every action that genuinely leaves Workshop.

Use the owner image
`C:\Users\peter\iCloudDrive\Documents\Career\Website\Changes\Workshop\8.png`
as discussion context only unless it is deliberately copied into the package,
hash-pinned, and locked by Pete.

### Build Your Future

Create exactly two meaningfully different Goal Board directions:

1. a quiet professional whiteboard using tasteful, legible handwriting; and
2. an editorial goal wall that avoids bubbles, card grids, or visual noise.

Both concepts must include the approved Goal lifecycle and controls:

- Short Term, Long Term, Work, and member-labeled Custom categories;
- a small Add Goal prompt with Goal text, optional soft ETA, optional private
  background, optional Feed proposal, Voice, Ask Slate, Save, and Cancel;
- a concise Board item and in-place private detail pop-out;
- explicit Mark Goal Complete;
- optional post-completion Feed, Story, and résumé-page proposals;
- progress, supportive Sparks, Try Another Future, and Small Experiments; and
- Future Postcard only as a deferred optional guided exercise, not a permanent
  Board section.

Do not add work-history cards, Projects as a Goal category, connecting lines,
zoom, arrange controls, current-focus markers, uploads, automatic publication,
automatic destination/canonical updates, streaks, punitive deadlines, failure
framing, or an unapproved fullscreen control.

## 4. Visual scrutiny gate

For every concept, compare each meaningful visible item against its approved
inventory row. Reject or revise any concept that:

- omits an approved essential item;
- introduces an unlisted meaningful item;
- duplicates another control or status without an approved Combine decision;
- misstates capability, privacy, audience, source, lifecycle, or destination;
- buries the dominant object under cards, decoration, or navigation;
- looks attractive but fails keyboard, visible-focus, contrast, touch-target,
  mobile, long-content, 200% reflow, reduced-motion, empty, loading, unavailable,
  denied, failure, or recovery needs; or
- sends the member away from the page without a distinct product reason.

Pete is the visual reviewer. Present side-by-side concepts with a concise
inventory-parity critique, strengths, weaknesses, and recommended direction.
Pete may select one, reject all, or request a deliberate hybrid. Do not call a
visual authoritative until its exact durable file and SHA-256 are recorded and
Pete explicitly locks it.

## 5. Bounded implementation-information packages

After exact visual locks, prepare two separate packages:

1. Workshop implementation information; and
2. Build Your Future / Goal Board implementation information.

Do not combine the packages merely because they share the Studio shell. Each
package must define:

- exact route/surface and shell ownership;
- page-purpose and locked visual references/hashes;
- meaningful item-to-capability/state mapping;
- same-page versus off-page interaction rules;
- source, canonical truth, proposal, projection, privacy, audience, and
  lifecycle boundaries;
- owner-derived authorization before retrieval;
- loading, empty, unavailable, denied, failure, retry, long-content, many-item,
  mobile, keyboard, touch, 200% reflow, and reduced-motion states;
- accessibility, security, observability, rollback, and feature-flag needs;
- backend/data dependencies and explicit unsupported behavior;
- focused and broader test/evidence requirements;
- homepage-impact assessment; and
- exclusions, later work, and owner decisions.

Build Your Future additionally needs a deliberate Goal create/edit/complete/
archive/delete/share data and concurrency contract. Workshop additionally needs
an exact Workbench draft/resume-page-proposal/evidence/destination-handoff
contract. Architecture is added only where those unresolved contracts genuinely
require it.

## 6. Agent routing

- ChatGPT creates and materially revises visuals; Pete scrutinizes and locks.
- If architecture is genuinely needed, use GPT-5.6 Sol Extra High.
- Later bounded implementation uses GPT-5.6 Terra Extra High.
- Later independent review uses one GPT-5.6 Sol High reviewer where risk
  justifies it.
- The equivalent Claude structure remains Fable architecture, Sonnet 5
  implementation, and Opus review when Claude is the selected ecosystem.
- Do not duplicate an accepted architecture, writer self-review, or independent
  review without a named critical reason.

## 7. Required session handoff

Return:

- exact branch, base SHA, and current SHA;
- visual files created and their status: candidate, selected, rejected, hybrid,
  or Pete-locked;
- inventory-parity and accessibility/state findings for each concept;
- exact Pete decisions and locked hashes, if any;
- the two bounded package paths and unresolved decisions;
- tests/evidence performed;
- confirmation that Claude's active lane and all runtime files were untouched;
  and
- the single next action.

If visual selection is not complete, stop at the visual gate. Do not compensate
by starting architecture or implementation.

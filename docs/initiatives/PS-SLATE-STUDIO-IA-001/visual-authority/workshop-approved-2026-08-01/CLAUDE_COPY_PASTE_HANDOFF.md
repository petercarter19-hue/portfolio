# Copy/paste prompt for Claude

You are receiving Pete's approved ChatGPT/Codex visual-correction set for
PeerSlate Workshop, package `PS-SLATE-STUDIO-IA-001`.

Start with the repository's mandatory gate: fetch authoritative Azure DevOps
`origin/main`, read `START_HERE.md` and
`docs/governance/CURRENT_BASELINE.yaml`, classify the work, and preserve every
unrelated change. Then read the current Workshop package and the visual
integrity standard. The relevant existing architecture worktree is
`/Users/petercarter/portfolio-workshop-architecture`; inspect its current
branch/status before writing and do not adopt, reset, merge, or overwrite it
without verifying ownership and current authority.

Read `00_READ_ME_FIRST.md` in this visual package before inspecting the images.
Files `03`–`10` are Pete-approved corrected visual direction. Files `01`–`02`
are composition reference only, with their obsolete navigation, browser chrome,
competing actions, and Type/Speak interaction explicitly superseded.

Your task is architecture and implementation planning, not new visual design.
Translate the approved visual promises into a bounded product/technical
architecture and phased implementation contract. Do not materially redesign
composition, hierarchy, typography, color language, responsive interaction, or
voice states. Return any material visual conflict to ChatGPT and Pete.

Architecture must preserve:

- private-by-default, server-derived identity, and authorization before reads or
  writes;
- separate source wording, AI interpretation/proposal, confirmed canonical
  information, and downstream projections;
- explicit review of exact wording before `Save privately`;
- `Save unfinished` as a private draft, not confirmation;
- no silent AI save, publication, sharing, deletion, or downstream mutation;
- member-controlled Work/Personal/Both classification and per-item AI-use
  permission;
- one voice-capable composer everywhere meaningful text is authored, with text
  fallback and the approved recording/transcribing/retry/permission states;
- no Type/Speak tabs;
- no resume-document builder; only future explicit updates to existing
  PeerSlate destinations;
- Workshop and Interview Studio coexisting in navigation;
- Role/Qualification Alignment remaining a separate focused workflow, with
  Workshop limited to strengthening missing evidence.

Before implementation, reconcile one explicit conflict with Pete: these
approved visuals show AI context permission off by default, while earlier owner
record `16_OWNER_DECISION_RECORD_WORKSHOP_2026-08-01.md` states that private AI
suggestion permission defaults on. Do not silently choose either behavior. The
route/URL also remains undecided.

Return:

1. the reconciled architecture decision record;
2. bounded data, authorization, provenance, AI-permission, voice, and
   downstream-use contracts;
3. a screen/state-to-contract mapping for approved files `03`–`10` plus the
   corrected opening/direct-entry behavior;
4. an implementation/test/release sequence with honest current-vs-future
   capability boundaries;
5. exact ownership, branch/worktree, review, and acceptance gates required by
   the current package and repository authority.

Do not claim Workshop is implemented, deployed, or live. Do not begin runtime
implementation unless Pete separately authorizes that next phase after the
architecture is reconciled.


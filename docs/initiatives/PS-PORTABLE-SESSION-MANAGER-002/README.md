# PS-PORTABLE-SESSION-MANAGER-002 - Durable Tool Routing

## Authority

- Owner decision: Pete, 2026-08-16
- Package manager and sole writer: root Codex site-audit session
- Branch: `work/2026-08-16-portable-session-manager-002`
- Base: `bdb9de227418f770d2fbd1d386e8c0caf6a6f5c9`
- Parent: completed `PS-PORTABLE-SESSION-MANAGER-001`
- Path: Protected, non-production shared governance
- Visual and architecture work: not triggered
- Specialist return target: package manager; no specialist is triggered here
- Fresh exact-SHA review: required before merge because shared governance changes

Codex manages this package because its outcome is limited to repository policy
and deterministic guardrails; ChatGPT Work adds no material value here.

## Outcome

Make the accepted tool-routing rule visible to every new PeerSlate repository
session or machine. `docs/AI_MODEL_AND_ROLE_ROUTING.md` is the one detailed
authority; `AGENTS.md` and `CURRENT_BASELINE.yaml` point to it.

The rule is simple:

- The overall site-audit manager owns the global map and dispatch.
- Each newly activated or transferred package has one outcome-selected manager;
  older active work records one at its next handoff.
- ChatGPT Work manages substantial experience packages when connected files,
  signed-in/on-screen review, device, voice, responsive, owner-decision, or
  multi-specialist evidence adds real value.
- Codex manages bounded repository and technical packages. Claude Co-Work
  manages only when explicitly assigned.
- Original ChatGPT alone creates material visual direction and returns Pete's
  accepted artifacts to the package manager.
- Manager, architect, writer, reviewer, and release authority remain separate.
  Routine work may stay self-managed when another layer adds no value.

## Scope and completion

Authorized changes are the central routing authority, one startup pointer, one
baseline pointer, the decision log, this package, registry accounting, and one
small guardrail set. Completion requires preflight, focused tests, diff
review, and a fresh exact-SHA review.

This package changes no application, Interview AI, prompt, model, auth, schema,
infrastructure, configuration, visual, deployment, production data, or live
member behavior. It grants no credential, merge, release, or production access.

# Current state (read-only inspection, 2026-07-16)

- Branch at start: `main` @ `39df50f9` (clean apart from the two untracked Bible docx files).
- Remotes: `origin` = Azure DevOps (source of truth), `github` = mirror. Azure Pipelines deploys `main` to the `peerslate-pete` Web App (`azure-pipelines.yml`).
- **Homepage:** `/` → `home()` → `templates/experience.html` (the five-scene cinematic page; own CSS `static/css/cinematic.css`, JS `static/js/cinematic-home.js`). `/experience` renders the same template — established precedent for keeping an old homepage reachable for comparison/rollback.
- **Global header/footer:** rendered by `templates/base.html` for every page. Logged-out header: logo → `/`, Pete's Slate → `/petec/resume`, Community → `/the-slate`, Interview Studio, About PeerSlate, header search, Ask AI, Sign In. `body.peerslate-home-page` applies the light homepage header treatment. The profile sub-header is not rendered at `/`.
- **URL helpers:** context processor provides `portfolio_url(...)`, `portfolio_story_url` (`/petec/my-story`), `portfolio_skills_url`, `portfolio_resume_url`, `portfolio_experience_url` (`/petec/resume#experience`), `portfolio_home_url`.
- **My Story:** `/my-story` + `/petec/my-story` → `my_story()` loads `static/data/story_data.json` into `my_story.html`. Four acts with stable section anchors `id="{{ act.id }}"`: `#act-now`, `#act-becoming`, `#act-life`, `#act-next`. All content needed by the scene exists as structured cards (ids: `now-maui`, `now-purpose`, `now-currently`, `now-note`, `now-turning`, `ch-pizza`, `ch-36`, `ch-airforce`, `ch-industry`, `life-race`, `life-bali`, `life-hawaii`, `next-closing`, `next-focus`, `next-toward`). Images live in `static/images/story/` with `-m` mobile variants and authored alt text.
- **Slate Board:** `/slate-board` + `/petec/slate-board` → `slate_board()`.
- **Résumé data:** `static/data/resume_data.json` — `metrics` by id (`engineers-led` 30+, `redesigns` 9 / $19.2M, `contract` $36M+, `repair-test` 70%, `issue-time` 35%), `career_roles` (`dod` 2021–2024, `l3harris` 2024–2025, `northrop` 2025–Present with `employer`, `focus_tags`), `skills` (with `display_name`, `evidence_items`, `featured`), `education` (`credential`, `institution`, `status`, incl. PMP and the admitted USA Ph.D.), `profile` (name, positioning, headshot path).
- **Feed design preview:** `/feed-living-stream` (public, labeled, with a working `?state=voice` capture demo) — a real destination for the voice hero CTA.
- **CSS architecture:** page-scoped stylesheets loaded via `extra_head` (`cinematic.css`, `people-interests.css`, `feed-living-stream.css` precedents). Tokens: Product Indigo `#4F5BD5`, Connection Azure `#4EA3FF`, AI Cyan `#2EC8D3`, Evidence Amber `#D7A33E`, Midnight Ink `#0A1B36`, Cloud White `#F6F8FC`. Fonts already loaded site-wide: Inter, Source Serif 4 (editorial serif), Playfair Display. `.ps-home` class is already used by the OLD homepage styles — must not be reused.
- **Tests:** `tests/test_navigation.py` pins header links/search records; `tests/test_my_story.py`, `tests/test_site_background.py` etc. exist. Suite: 171 passing before this change.
- **Files that must not change:** `templates/base.html` navigation, `my_story.html` behavior (other than none), résumé pages, Slate Board, auth, Azure config.
- **Mockup-vs-production conflicts:** mockup header (How It Works / Explore a Slate / For Teams / Pricing / Create My Slate) is explicitly NOT to be implemented; "Trusted by Google/Microsoft/…" logo row would fabricate endorsements (dropped — see 08-decisions.md); mockup Polaroid captions are illustrative (live captions used instead); mockup résumé-card tabs (Summary/Impact/…) would be dead controls (rendered as a static preview instead).

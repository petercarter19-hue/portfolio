# PS-AI-OPS-CHECKPOINT-001 - Three scoped corrections

**Current state:** `Conditional` on three affected surfaces; no global delivery
hold.
**Assessed build:** Azure main
`b8e9e26ba0e8cb2bc93fa936c4ddd7985e9f72fb`, 2026-07-29.
**Historical evidence:** counted slice chronology, review evidence, and the
original decision remain in `OWNER_TECHNICAL_COMPLETION_REPORT.md` and
`docs/governance/AI_DELIVERY_AUDIT_REGISTER.md`.

## Open corrections

1. **Candidate admission** - replace the hard-coded historical branch selector
   with auditable package-specific exact-SHA admission. This blocks only a
   future Protected release that uses Candidate.
2. **Work & Impact profile provenance** - bind overlay content and style
   eligibility to the owning profile and add a generic second-profile
   regression. This blocks only release/expansion of the affected overlay.
3. **Interview follow-up mode provenance** - sign/bind the selected mode,
   reject mismatches, keep grounded prior-answer content out of the generic
   provider message, and add the provider-message regression. This blocks only
   release/expansion of the affected follow-up path.

## Current rule

Each defect remains open until its owning package corrects it and focused
verification passes. Routine or Bounded work outside those surfaces may
proceed. The historical four-slice counter is evidence only; it no longer
schedules audits, resets a cadence, or creates a stop-the-line gate.

This package has no runtime authority. A correction needs its own owner,
branch, scope, focused tests, and review proportional to that risk. One focused
recheck may close all three when the exact corrections are ready.

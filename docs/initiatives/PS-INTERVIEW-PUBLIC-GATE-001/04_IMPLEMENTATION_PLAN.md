# PS-INTERVIEW-PUBLIC-GATE-001 — Implementation Sequence and Handoff

## Safe sequence

1. Fetch `origin`, confirm PS-NEXT-WAVE-MANAGER-001 is on current main with a green pipeline, and record the full base SHA.
2. Create the Interview Studio task branch and inspect the public routes, current DOM/JavaScript state model, focused tests, and live desktop/mobile behavior.
3. Record before screenshots and a concise diagnosis of identity ambiguity, browser-state labeling, transmission disclosure, and default-view density.
4. Add or update focused tests for the route/truth contract before or with implementation.
5. Refine server-rendered labels and hierarchy first, page-specific CSS second, and JavaScript only where needed for state labels, disclosure, focus, or fallback behavior.
6. Exercise all existing modes and negative states without changing backend contracts.
7. Run focused tests, guardrails, the full configured suite, `git diff --check`, and a reserved-file audit.
8. Capture after evidence, complete the standard report, commit, push, and hand the designated session manager the branch plus exact full SHA. Do not merge your own lane unless the manager explicitly asks.

## Stop and ask the manager when

- the result requires `app.py`, an API contract, authenticated route, session logic, database persistence, or account sync;
- a shared base/theme/nav file appears necessary;
- a mode currently sends or stores more data than the package contract describes;
- private or non-public profile data appears in the public payload;
- another active branch owns a required file;
- the work expands into a wholesale redesign or owner Studio implementation.

## Paste-ready kickoff

> Open and follow `START_HERE.md`, then the current governance records, Bible/Roadmap/Sync Standard, Site Rules, PS-INTERVIEW-002, and every file named by `docs/initiatives/PS-INTERVIEW-PUBLIC-GATE-001/README.md`. You are the Claude Code public-experience writer. Confirm the complete Gate 2.4 package, Claude/Fable feasibility review, and Pete/designated-session-manager visual approval are recorded, fetch `origin`, and create `work/<today>-interview-public-gate-001` from current `origin/main`; record the full base SHA. Make `/interview-studio` an unmistakably honest public demonstration with clear named public-profile grounding, current-browser storage labels, accurate coaching/media disclosures, and progressive layering around one dominant practice object. `/interview-studio/history` remains browser-local; `/app/interview-studio` is reserved for a future authenticated owner workspace and must not be created or simulated. Stay inside the reserved template/CSS/JS/test files; do not touch routes, APIs, auth, database, Capture/Moment, résumé, global nav/theme, or deployment. Preserve all existing written/AI/compare/history/camera behavior and fallbacks, prove desktop/mobile/keyboard/zoom/reduced-motion/no-JS truthfulness, keep focused and guardrail tests green, complete the standard report, push, and hand the designated session manager the branch plus exact full commit SHA for review.

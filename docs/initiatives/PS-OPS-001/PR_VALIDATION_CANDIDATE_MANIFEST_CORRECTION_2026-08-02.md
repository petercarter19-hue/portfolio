# PS-OPS-001 PR validation Candidate manifest correction - 2026-08-02

## Core record

- **Task/package and delivery path:** `PS-OPS-001`; Protected shared-infrastructure correction, integrated only in the existing auth alias-scope worktree because that is the sole active writer for the reviewed Candidate-manifest support.
- **Outcome and member/site effect:** The immutable Candidate-artifact validator runs for every successful Build source. The reviewed script accepts only an exact Azure synthetic ref, `refs/pull/<positive-decimal-id>/merge`, as a disabled-admission PR validation and rejects any Candidate input on that form; tags and other malformed refs still fail. This changes no runtime behavior, configuration, secret, data, identity, Candidate resource, deployment, or production state.
- **Authority and integration lineage:** Run 352 first exposed the issue: synthetic source `5c58dc5bad301e1fe77884e877f4aa6ba53f4d51` used `Build.SourceBranch=refs/pull/245/merge`, so the older script rejected its manifest while Deploy and every Candidate stage stayed skipped. Azure PR 248 then validated the strict correction in run 358 and squash-integrated it as `4ce01713476b5d4c3fe802a70364e13652fd7949`, which contains the reviewed strict script and PR-ref tests. Azure PR 246 subsequently advanced authoritative `origin/main` to `58d95723d42103be95ee51b3e85a22c0be5bcb63` and added an eleven-line condition/comment that skipped the validator unless `Build.SourceBranch` began `refs/heads/`. The branch merged that exact main commit without rebase or rewrite as `0096f080c1113a19c1c954044c0f0e9b5d185d9c` (parents `4ce01713476b5d4c3fe802a70364e13652fd7949` and `58d95723d42103be95ee51b3e85a22c0be5bcb63`), then removes only that skip in the follow-up correction.
- **Why the PR 246 condition was fail-open:** A manual `refs/pull/.../merge` or tag build with matching `candidatePackage`, `candidateSourceBranch`, and `candidateSourceVersion` could skip the validator yet satisfy the existing CandidateDeploy, CandidateSmoke, and CandidateStop equality conditions. The skipped script was the only component that rejects Candidate inputs on PR refs and invalid source refs, so its absence left later stages able to trust unvalidated queue metadata.
- **Final source-diff reality:** PR 246's Owner Home golden repair is inherited main work. The merge conflict in `tests/test_owner_home.py` was resolved to the exact `origin/main` blob `ae26d299e97e9b99d0e365d2e037d0b6e9622ce6`; it is not changed relative to `origin/main` and this correction does not re-own it. The final branch diff retains the reviewed Candidate script/tests and auth evidence already on the branch; this follow-up changes only `azure-pipelines.yml`, `tests/test_operational_readiness.py`, this record, and the auth completion record needed to describe that inherited collision.
- **Verification performed and result:** Completed PR 248 run 358 used synthetic source `3a034805e1aeae58030512171fd68cbc12718604`: 1,401 tests with 19 skips, dependency/Gitleaks/compile checks passed, and its disabled-admission manifest recorded artifact SHA-256 `4550a723c6e422f14a78cd979155df1f66e9ffdf7e6a2cc0f559bb3904df44f9`; all production and Candidate stages were skipped. For this post-PR-246 follow-up, focused auth/ops/golden/release-template tests passed 199 tests (including all staged PowerShell parser checks); `tests.test_operational_readiness` passed 25 tests, including the new unconditional-task structural proof; focused compile, `pip check`, and `git diff --check` passed; and `ANTHROPIC_API_KEY=test-placeholder` full `python -m unittest -q` passed 1,401 tests with 4 skips.
- **Release state:** PR 248/run 358 is a completed historical PR-validation result only, not deployment evidence. This post-PR-246 follow-up is pre-merge local only: not pushed, no new PR, Azure interaction, Candidate run, deployment, production/configuration, or identity mutation.
- **Known limits, deferred work, or owner decision needed:** This does not close the separate package-specific Candidate-admission finding. Candidate admission remains fail-closed and needs its own reviewed correction before a future Protected release uses Candidate.
- **Next action:** Complete the required local validation and self-review, then hand off the exact local commit for ordinary Azure PR review; no pipeline is queued by this task.

## Protected additions

- **Shared infrastructure / rollback evidence:** No live system changed. Do not undo this safety correction by restoring PR 246's ref/reason skip: that would recreate the unvalidated-metadata path. If a later regression requires a rollback, use a narrow reviewed compensating commit that keeps the validator unconditional, and verify the strict PR-ref/empty-input cases again. The auth integration merge itself is preserved; no rebase, force rewrite, or production recovery action is authorized.

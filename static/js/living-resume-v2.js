document.addEventListener('DOMContentLoaded', () => { const tabs=[...document.querySelectorAll('[data-chapter]')]; const panels=[...document.querySelectorAll('.lr-chapter')]; const select=id=>{tabs.forEach(tab=>tab.setAttribute('aria-selected',String(tab.dataset.chapter===id)));panels.forEach(panel=>{const active=panel.id===`chapter-${id}`;panel.hidden=!active;panel.classList.toggle('is-active',active)});}; tabs.forEach(tab=>tab.addEventListener('click',()=>select(tab.dataset.chapter))); document.querySelectorAll('[data-constellation-target]').forEach(button=>button.addEventListener('click',()=>{select(button.dataset.constellationTarget);document.getElementById('ledger').scrollIntoView({behavior:matchMedia('(prefers-reduced-motion: reduce)').matches?'auto':'smooth'});document.getElementById(`tab-${button.dataset.constellationTarget}`).focus({preventScroll:true});})); });

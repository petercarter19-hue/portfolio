# Pete & Danielle Carter — Portfolio Project Memory

## Who I'm working with
- **Pete Carter** — building his first website, beginner coder on Mac (MacBook Pro 14" M5)
- Email: petercarter19@gmail.com
- GitHub username: petercarter19-hue
- GitHub repo: https://github.com/petercarter19-hue/portfolio.git
- Location: Athens, AL

## The Project
**Personal Portfolio Website** — a Python Flask web app with AI features powered by the Claude API.

Two goals:
1. Demonstrate real, hireable tech skills
2. Create a live public-facing site to share with employers

## Pete's Credentials (to feature on the site)
- PMP certification
- PhD
- MBSE certification
- Work experience & career history
- Smart home & hobbies

## Planned Pages
1. **Home / About** — bio, intro
2. **Work Experience & Career**
3. **Skills & Certifications** (PMP, PhD, MBSE)
4. **Hobbies Showcase** — smart home projects
5. **Contact Form**
6. **Private Admin/Assistant Dashboard** — login required

## AI Features Planned
1. **Visitor Chatbot** — floating widget on all pages, visitors ask questions about Pete, answers from his resume/bio, streams responses
2. **AI Writing Tool** — takes bullet points, returns polished professional paragraphs (behind the scenes)

## Tech Stack
- **Backend**: Python + Flask
- **AI**: Anthropic Claude API
- **Frontend**: HTML/CSS/JavaScript (templates via Flask/Jinja2)
- **Version Control**: GitHub

## VS Code Setup (completed 2026-06-25)
- VS Code installed ✅
- Portfolio folder opened and trusted ✅
- Extensions installed: Live Server, Prettier, Auto Rename Tag, GitHub Pull Requests, PowerShell, Claude Code, and others ✅
- venv (Python virtual environment) created inside /Users/petercarter/portfolio ✅

## Current State of Project
- Portfolio folder: /Users/petercarter/portfolio
- Flask app is running from app.py with routes for home, about, work, skills, hobbies, and contact
- Site runs at http://127.0.0.1:5000 with 6 routes (home, about, work, skills, hobbies, contact)
- Chatbot MVP 1 is active: static/js/chatbot.js sends visitor questions to the Flask /api/chat route, and app.py calls the Anthropic Claude API
- Chatbot answers are intentionally short: 1 to 3 concise, polished sentences with the most impactful information first
- Chatbot answers should use plain text only: no Markdown, hashtags, bold markers, bullet lists, numbered lists, or salesy follow-up questions
- Chatbot responses can use a second short paragraph when an answer has more than one idea
- app.py includes clean_chatbot_reply() to remove display-only Markdown symbols before sending responses back to the browser
- app.py keeps knowledge files separated and uses build_knowledge_context() to send Claude only the most relevant knowledge files for each question
- Suggested chatbot questions now disappear after any visitor question, whether the user clicks a suggestion or types a custom question
- Knowledge base complete: docs/knowledge/ folder with 6 files
- chatbot.css uses white-space: pre-line so paragraph breaks from AI responses display inside chat bubbles
- anthropic and python-dotenv installed in venv
- .env file with ANTHROPIC_API_KEY exists on this Mac and must never be displayed, edited, or committed

## Immediate Next Steps
1. Continue testing MVP 1 chatbot answer quality with real recruiter-style questions
2. Consider creating a curated chatbot-specific knowledge file so responses rely on polished source material instead of long resume-style files
3. Add server-side logging of conversations
4. Build admin dashboard to view chat logs
5. Eventually: smarter retrieval, security hardening, and public deployment

## Recent Session Updates (2026-07-08 — Slate consolidation + mobile nav, remote, branch claude/the-slate-mockup-integration-tviz2h rebased onto merged main again)
- BRANCH: PR #9 merged; this round rebased onto updated main → new draft PR. Same branch name reused after each merge (never reuse the merged PR).
- PROFILE TAB RENAME: the profile-strip "My Slate" (next to Overview) is now "My Story" (disambiguates from The Slate's own "My Slate" goal-map tab). URL still /petec/my-story. Page eyebrow + <title> + base.html search index updated. (partials/profile_tabs.html, my_story.html, base.html)
- OVERVIEW ASK REVERTED: the Ask Pete AI field moved OFF the tab strip and BACK into the dark "Ask Pete's Slate" bar in the profile_preface block (3 chips: Summarize Pete / Match to Role / Interview Pete — Show Systems Evidence stays dropped; no metric cards). profile_tabs.html is now just the 6 tabs (no LinkedIn, tighter) wrapped in .profile-tabs__list[data-mobile-tabsource].
- THE SLATE TABS restructured (partials/the_slate_head.html): Slate Feed / **Slate Board** (links out to /petec/slate-board, between Feed and My Slate) / My Slate / Daily Slate. Slate Paths tab REMOVED — merged into My Slate. Route the_slate_paths now 302-redirects to the_slate_my (endpoint kept so url_for still resolves). ts-tabs got data-mobile-tabsource.
- FEED LAYERS merged (partials/slate_feed_tabs.html): People + Progress → one "People & Progress" landing (/the-slate). Layers now People & Progress / Pulse / Break. slate_feed route (/the-slate/progress) 302-redirects to the_slate (endpoint kept). the_slate_daily.html lost its stray feed-layer strip.
- MY SLATE is now a MERGED DASHBOARD (templates/the_slate_my.html) matching Pete's mockup — goal map (unchanged) + rail, then stacked full-width sections: **My Paths** (3 ts-pathcard), **Today's Goals** (PMP ts-miles, "4 of 6 completed") + **Daily Slate Check-in** (ts-checkin, 6 quick chips Studied/Practiced/Worked Out/Read/Built/Other — the-slate.js logs into Recent Activity's [data-ts-updates]), **People & Progress** (ts-pp: My Network sub-tabs + 3 cols Recent Activity / Progress Highlights (.ts-highlights) / Achievements (.ts-ach)), **Upcoming Milestones** + **Recommended for You** (.ts-reco 4 cards). Intro gained "5 Active Paths" stat + Filters + a Dashboard sub-nav (.ts-subnav, anchors to sections). New CSS block "MY SLATE — merged dashboard" in the ts-* section. NOTE: Progress Highlights uses dedicated .ts-highlights (NOT .ts-heat) because the heatmap tiles set .ts-heat__body{display:contents} + grid placement which would break single-row layout.
- MOBILE NAV (#5): (1) header already sticky (position:sticky top:0 z-1200) — stays. (2) NEW always-on "Ask AI" button in the header (.nav-ask-ai, phones only; desktop keeps the search box's ask fallthrough) — opens the chat via #chat-toggle.click(). (3) NEW bottom tab bar (.mobile-tabbar, phones only, fixed bottom): static/js/mobile-nav.js CLONES the page's [data-mobile-tabsource] links (profile tabs on /petec, ts-tabs on /the-slate) into it and reveals on scroll-up / hides on scroll-down (translateY). body gets padding-bottom + #chat-toggle lifted to bottom:70px on phones so nothing is covered. Loaded in base.html (?v=mobile-nav-1). Data-source attrs added to profile_tabs .profile-tabs__list and the_slate_head .ts-tabs.
- Cache-busters: style.css ?v=slate-merge-1 (base.html). Verified (Playwright): merged My Slate + Overview + feed at 1280 Light + Dark Slate + White-stone, and 390 phone (bottom bar mirrors the right tab set, header Ask AI present); 22/22 interaction assertions passed; no console/page errors.

## Recent Session Updates (2026-07-08 — My Slate + Overview refinements, remote, branch claude/the-slate-mockup-integration-tviz2h rebased onto merged main)
- BRANCH NOTE: PR #8 (The Slate hub) MERGED into main mid-session. This follow-up round was rebased onto the updated main (git rebase origin/main; 9186300 was a real ancestor) and pushed as a NEW commit → a NEW draft PR. Never reuse a merged PR.
- MY SLATE (my_story.html): "Things You Won't Find on My Résumé" is now FIVE compact tiles in one row (JS VISIBLE_COUNT=5, rotation cycles 5 slots; grid repeat(5) → 3 at ≤900 → 2 at ≤620). Vertical mini-tiles (icon on top).
- COMPLETED GOALS on the journey timeline: opt-in "Add a completed goal" affordance (dashed toggle) opens a form (title + Public/Private toggle + Add/Cancel + hint). Adds a green-check timeline-goal card, saved per-browser (localStorage myStoryCompletedGoals, XSS-safe via textContent, deletable). static/js/story.js runs it (loaded via my_story extra_scripts, ?v=story-1). Spans full journey width (grid-column 1/-1) below the 4 career milestones (which stay fixed résumé history). GOTCHA fixed: both .timeline-goals__form AND .timeline-goals__toggle need `[hidden]{display:none}` restatements (their display:grid/inline-flex beat the UA hidden rule — same gotcha as ts-ai__plan + theme dropdown).
- OUTSIDE OF ENGINEERING → "OUTSIDE OF WORK" (.story-card--interests); removed the "More About Me" link; interest icons 24→34px (svg 14→20), h3 0.66→0.8rem, p 0.58→0.7rem; card padded tighter/centered so it's a little shorter.
- COLLAGE now feathers its edges into the page on ALL themes (radial mask); the old slate-light "hard framed photo" (border + shadow) was replaced with a slightly tighter feather so it blends on white too; light keeps the base -72px pull-up.
- VALUES are now LIFE-related (Family First / Health & Fitness / Lifelong Growth / Adventure) in a SHARED partial partials/life_values.html, included on My Slate AND the Overview. GOTCHA: the .drive-card uses the old --color-* tokens which are only remapped inside .my-story-page; on the Overview (outside that scope) they'd be near-white on white, so added `body.slate-light .drive-card h3/p/__icon` overrides to the shared --text/--text-muted/--accent tokens (dark/stone themes have no slate-light class, so they keep the theme palette).
- OVERVIEW (index.html): REMOVED the 4 metric cards + the whole profile_preface block (metrics + dark ask bar gone). Life-values now sit in a .ps-card block ABOVE Core Skills. "View full timeline" (Experience block) → resume `#resume-career-section` (also the My Slate "View Full Timeline").
- TAB STRIP (partials/profile_tabs.html): LinkedIn logo removed; tabs live in .profile-tabs__list (tighter gap clamp(14px,2.3vw,30px)); on the OVERVIEW ONLY, the shared Ask form (partials/portfolio_ai_search.html, #hero-ai-input) is pinned right of Resume as .profile-tabs__ask (label+status hidden, field ~232px = "half as wide", answer floats absolute below so the sticky bar doesn't grow); 3 quick chips in a slim .profile-tabs__chips row (Summarize Pete / Match to Role / Interview Pete — "Show Systems Evidence" dropped). Gated to overview paths so other pages don't render a duplicate #hero-ai-input. At ≤860px the ask drops to its own full-width row. Chip-prefill JS (index.html extra_scripts) still targets these moved elements; chatbot.js clears the field after submit (by design) and its pageAskAnchor list gained .profile-tabs__ask.
- ABOUT PETE (profile_shell.html): rewritten to blend career + personal ("Systems engineer by trade, lifelong learner by nature… running trails, traveling with my wife, tinkering with our smart home"). CSS: max-width 320→430px (wider), padding 18/20→14/18 + tighter line-height (shorter) → more vertical space in the band.
- DARK SLATE STONE ask field (O3): the tab-strip ask box was cut from the same dark stone and blended; scoped rule lightens it to a warm champagne wash (rgb(78 71 57/60%)) over the stone photo + a gold hairline + soft gold glow so it draws the eye. Only `body[data-theme="stone-slate"] .profile-tabs__ask .hero-ai-search__box`.
- SITE-WIDE LIGHT THEME: every card is slightly raised (soft blue-tinted shadow) with a thin light-blue hairline (#cbdcf8). One broad `body.slate-light` selector list (ps-card + the story/profile/resume/work/interview/board/feed/contact families), placed after the per-page slate-light rules so it wins; homepage never takes slate-light so it's untouched.
- Cache-busters bumped in base.html: style.css ?v=refinements-1, chatbot.js ?v=refinements-1. Verified (Playwright): My Slate + Overview in Light / Dark Slate / White-stone + 390 phone; 21/21 interaction assertions passed; no console/page errors.

## Recent Session Updates (2026-07-08 — THE SLATE hub (4 mockups → 4 tabs), remote, branch claude/the-slate-mockup-integration-tviz2h)
- NEW MAIN NAV LINK "THE SLATE" (/the-slate) replaced the separate "Slate Feed" and "Slate Board" top-level links. Nav is now Home / The Slate / Example Slate / Interview Me / About. Pete's personal Slate Board page still exists at /petec/slate-board (Example Slate profile tab; "Example Slate" now highlights there too); only the top-level link moved.
- THE SLATE = one product hub, four internal tabs from Pete's four 2026-07-08 mockups: Slate Feed (/the-slate, People layer active), My Slate (/the-slate/my-slate, goal map), Daily Slate (/the-slate/daily), Slate Paths (/the-slate/paths). Shared page-head partial: partials/the_slate_head.html (title + subtitle + 12-day-streak pill + 4 tabs; set `ts_active` = feed|my|daily|paths before including). Product message: goals become progress → progress becomes proof → proof connects people.
- FEED LAYERS: the old feed pages MOVED under the hub — /the-slate/progress|pulse|break (endpoint names slate_feed/slate_feed_pulse/slate_feed_break kept so every url_for still works). partials/slate_feed_tabs.html is now the 4-layer strip (Progress/Pulse/Break/People — My Slate left it; People → /the-slate). Old /slate-feed* URLs 302-redirect. The old People view (slate_people.html + slate_people.json + its render route) was SUPERSEDED by the hub's People layer and deleted.
- NEW TEMPLATES: the_slate_feed.html (composer + 4 grouped people sections + AI-coach/heatmap/break/wins rail), the_slate_my.html (goal-map board: 6 goal cards + SVG connection wires + zone labels + live filters + legend; Suggested Next Step / People Linked / Goal Rooms rail), the_slate_daily.html (composer + 6 progress cards + 2-step-plan AI / trending paths / people rail), the_slate_paths.html (featured paths + expanded PMP path w/ milestones + WORKING check-in + path rail). Sample people/goals are consistent ACROSS tabs (PMP, Run 500, portfolio, home-lab write-up) — that cross-tab repetition IS the product concept; keep it coherent.
- CSS: new ts-* family in style.css, inserted ABOVE the SURFACE SLATE block (which must stay last — the new cards use .ps-card so they're auto-stone on White Slate/Dark Slate Stone). All theme-token driven, no per-theme rules. Cache-buster ?v=the-slate-1.
- GOAL-MAP GEOMETRY: the wires SVG uses viewBox="0 0 100 100" + preserveAspectRatio="none" + vector-effect: non-scaling-stroke, and cards are positioned by left/top PERCENTAGES — wires and cards share one coordinate space at any width. Canvas is min-width:820px inside .ts-board__scroll (overflow-x). GOTCHA: grid items default to min-width:auto — the `.ts-main > * { min-width: 0 }` rule is what keeps the 820px canvas scrolling inside its card instead of blowing the page open on phones.
- CSS COMMENT GOTCHA (cost a real bug): writing ".ps-tint--*/" in a comment ends the comment at `*/` — the garbage after it swallowed the next rule (.ts-head__row lost its flex). Never put a glob-star right before a slash inside CSS comments. Also: .ts-ai__plan needed a `[hidden]{display:none}` restatement (its display:grid beats the UA hidden rule — same gotcha as the theme dropdown panel).
- static/js/the-slate.js (loaded by all four tabs): composer (category chips, Public/Private toggle, 500-char counter) posts a REAL card to Daily Slate (localStorage key theSlateDailyCards, XSS-safe via textContent, deletable ×; posting from the Feed tab saves + links across), hearts toggle, AI cards rotate through slate-specific ideas (JSON script blocks) + "Not now" dismiss, goal filters really filter (data-zones) and "Focus this goal" spotlights the automate card, path check-in logs a "You · Just now" entry into Top recent updates. 20/20 Playwright interaction assertions passed.
- base.html: nav + header-search JSON index updated (The Slate entries added, Feed·People removed, Slate Board entry kept for Pete's board).
- Verified (Playwright, headless Chromium): all 4 tabs + the progress layer at 1280 in Light / Dark Slate / White Slate, plus 390 phone; no console/page errors (only sandboxed Google-Fonts fetch failures). NOTE: full-page screenshots make background-attachment:fixed pages look like the stone "ends" mid-page and text below goes dark — screenshot artifact only; real scrolling is fine (verified with viewport shots).

## Recent Session Updates (2026-07-08 — 4 themes + "surface slate" (every card/button = stone), Mac, branch main)
- THEME PICKER IS NOW 4: Light / White Slate / Dark Slate Stone / Dark Slate. New id `stone-slate` = "Dark Slate Stone".
- ROOT PROBLEM found: the mockup/Example-Slate redesign gave the ps-*/bd-*/iv-* components FLAT fills (`.ps-metric` = `color-mix(--surface 92%)`, `.ps-askbar` = `--askbar-bg`, chips = `rgb(255 255 255/6%)`, primary btn = satin `--slab-primary`). So on the slate themes only the PAGE background was real stone; every card/button on top was a flat panel. That's why "the dark theme isn't slate."
- NEW "SURFACE SLATE" SYSTEM (bottom of style.css, big commented block): when theme-preview.js sets `data-surface-slate="on"` (White Slate + Dark Slate Stone, never on the homepage), EVERY card/chip/button/ask-bar repaints its background from the SAME fixed `--slate-photo` at `background-size:cover; background-attachment:fixed` — identical recipe to the platform header/tab strip — so the stone lines up 1:1 with the page behind it (same crop/scale, no seams, no more mismatched button texture). A per-theme `--surf-wash` (+ `--surf-wash-chip`, `--surf-wash-btn`, `--surf-askbar`) tints each surface; `--slab-hi/lo` + `--slab-card/chip-shadow` (already per-theme) give edge light + raised shadow. Selectors are grouped (cards / chips / primary btns / ghost btns / ask bar / ask-chips). This REPLACES the satin `--slab-primary` button here — buttons are now accent-tinted stone.
- `stone-slate` is a gray-slate TWIN: it was added to gray-slate's palette selector (shared block), its own bg-image (slightly lighter wash `34%/58%` vs gray's `45%/68%` so the rock reads crisper), and the wordmark filter. All the theme-agnostic `body[data-slate-photo="on"]` rules apply automatically. On the HOMEPAGE, theme-preview.js renders stone-slate AS gray-slate (`renderedTheme`) because the home dark look is gated on `[data-theme="gray-slate"]`; the active-button highlight still keys off the true themeName.
- WHITE SLATE (paper-slate) is now WHITER: bg-image veil bumped to `rgb(253.../74%), rgb(250.../64%)` (was 55%/42%).
- FAINT TEXT on Dark Slate fixed: shared dark palette `--text-muted` #c8c1b3→#d8d2c6, `--color-text-muted` #a9a396→#c2bcae, `--color-text-secondary`→#e2ddd3 (helps gray-slate AND stone-slate).
- theme-preview.js: `useStone` now gray-slate|stone-slate|paper-slate; `surfaceSlate` = (!homepage) && (stone-slate|paper-slate) → sets `document.body.dataset.surfaceSlate`. Swatch `.theme-option--stone` (grey→gold) added.
- Cache-busters: style.css ?v=surface-slate-1, theme-preview.js ?v=four-themes-1.
- launch.json: Mac python + PORT 5001 (5000 held by macOS AirPlay/ControlCenter).
- Verified all 4 themes on /petec Overview (no console errors): Light flat white; White Slate whiter w/ white-slate cards+chips+buttons; Dark Slate Stone all dark-stone surfaces; Dark Slate flat cards + now-legible text. NOTE: surface slate uses many `background-attachment:fixed` layers — watch for scroll jank on low-end/iOS; fine on the M-series Mac.
- MERGE NOTE (2026-07-08): this landed on top of a large remote push (Slate Feed/Pulse/Break/People pages, header search, Interview Me revamp, light-page conversion). style.css merged as a union — remote's new page styles first, the SURFACE SLATE block kept LAST so it still wins the cascade over the new components (many of which use `.ps-card`, so they get surface slate too).

## Recent Session Updates (2026-07-07 — nav + light-page consistency + Pulse/Break feed pages, branch claude/portfolio-styling-nav-f1p55w)
- HOME LINK: the platform header "Home" link now points at Pete's Overview (portfolio_home_url, /petec), not the PeerSlate marketing splash — this is his personal site. To avoid two tabs lighting up on /petec, "Home" owns the bare overview paths and "Example Slate" now highlights only on the deeper portfolio sub-pages (base.html).
- PROFILE TABS reordered: My Slate moved from last to second, right after Overview (partials/profile_tabs.html). Order is now Overview / My Slate / Evidence / Projects / Slate Board / Resume.
- LIGHT-PAGE CONVERSION (the "backgrounds don't match, like my slate" fix): four legacy pages were still full dark-theme while the rest of the site is Light (body.slate-light default) — My Slate (.my-story-page/.story-hero), Projects (.work-page), Resume (.resume-page), Evidence (.resume-page.skills-page). They never got converted when the site went light site-wide. Fix = scoped `body.slate-light` overrides that (a) remap the old dark `--color-*` variables to the light palette (the old "gold" accent slots become the site blue #0b63e5), and (b) override the handful of HARDCODED dark backgrounds (page circuit art + card gradients) that don't come from variables. Work/story live at the END of style.css; resume/skills live at the END of resume.css. The My Slate hero collage (hero-collage.png, dark navy baked in) had its edge-fade mask dropped and is now presented as a rounded, bordered framed photo so it reads as intentional on white. Dark Slate theme (gray-slate) is untouched — those pages stay dark on the dark theme, which is correct.
- NEW FEED PAGES — Pulse & Break: the Slate Feed had five tabs (Progress / Pulse / Break / People / My Slate) but Pulse and Break were dead "coming soon" buttons. Both are now real pages. Routes slate_feed_pulse (/slate-feed/pulse) and slate_feed_break (/slate-feed/break) in app.py; templates slate_pulse.html (community momentum: this-week stat band, trending skills w/ .sf-bar momentum bars, what's-moving, rising-goals rail, this-week-in-numbers) and slate_break.html (recharge: encouragement hero, "ways to recharge" grid, community shout-outs, daily-spark quote band). Content is static/representative (PeerSlate is pre-launch, Pete is the only real member — same convention as the People view's sample cards).
- The five-tab feed strip was extracted into partials/slate_feed_tabs.html (pass `active` = progress|pulse|break|people) and all four feed views now include it, so the tabs can never drift apart again. Pulse/Break pages built entirely on the shared feed shell (.sf-page/.sf-tabbar) + design tokens (--surface/--border/--text/--accent) + .ps-card, so they match the Progress/People views and adapt to BOTH Light and Dark Slate with no per-theme work (verified in both).
- Cache-busters: style.css ?v=light-pages-1 (base.html); resume.css ?v=light-pages-1 (resume.html + skills.html).
- Verified in preview at 1280 desktop (all portfolio pages now consistently light, all feed tabs live) + 390 phone (Pulse/Break stack cleanly) + Dark Slate theme (Pulse). Flask ran locally with a dummy ANTHROPIC_API_KEY (pages render without calling the API).

## Recent Session Updates (2026-07-07 — White Slate theme revived, Mac, branch main)
- NEW 3rd THEME "White Slate" in the picker (Light / White Slate / Dark Slate). It is the old `paper-slate` theme brought back: it renders through the DARK theme's stone pipeline (data-slate-photo="on") using the WHITE stone photo (static/images/slate-backgrounds/light-slate-bg.jpg) under a light ~55% wash, so every white surface (page + cards) is cut white slate instead of flat white — while keeping the exact LIGHT (slate-light) color scheme.
- HOW IT WORKS: the theme id stays `paper-slate` internally (114 CSS refs + all the LATE light-stone override blocks were already wired for it — it was only removed from the button + JS). Three edits revived it: (1) theme-preview.js `useStone` now true for `gray-slate` OR `paper-slate` (so it gets data-slate/data-slate-photo="on" and does NOT get the flat `slate-light` class); (2) base.html added the "White Slate" button (data-theme-option="paper-slate") between Light and Dark Slate; (3) style.css retuned the `body[data-theme="paper-slate"]` palette block (~line 4325) from the old #2f6fe0/#16213a to slate-light's exact tokens (--accent #0b63e5, --text #061a3a, --border #d9e2ec, navy --askbar-bg, Source Serif via default). Also: the Light button now uses `.theme-option--light` (recolored white→#0b63e5, was a stale green), and `.theme-option--paper` swatch is stone-white→blue.
- Cache-busters: style.css ?v=white-slate-1, theme-preview.js ?v=three-themes-1.
- launch.json: switched back to Mac (`venv/bin/python`) and moved to PORT 5001 (macOS ControlCenter/AirPlay holds 5000) — added an `env.PORT=5001`. app.py already reads PORT. Will flip-flop with Windows as before.
- Verified in preview: all 3 themes switch cleanly (Light=flat white, White Slate=white stone + blue palette, Dark Slate=dark stone + gold, unchanged), no console errors, cards render as raised white-slate slabs on Overview.

## Recent Session Updates (2026-07-06 late — FULL MOCKUP REDESIGN (static/Mockup), Mac, branch main)
- Pete's high-res mockups live in static/Mockup/*.png — THE design source of truth: serif display type, white cards w/ hairline borders + whisper shadows, royal blue #2f6fe0 accents, navy #101f42 chrome, pastel icon circles, colored category flags.
- FONTS SITE-WIDE: h1/h2/h3 are now SOURCE SERIF 4 globally (loaded in base.html; exceptions: lifecycle stage labels + medallion stay Inter). Whiteboard handwriting switched Kalam → CAVEAT. Serif also on: profile name/title, metric values, board stat numbers, goal percents, interview question + score.
- THEMES: modern-blue is REBUILT as the mockup theme (old navy version gone) — cool paper page, flat white cards (photo pipeline w/ ~92% white wash so only the faintest texture shows). paper-slate is now "WHITE SLATE": identical palette but the light stone photo shows through (~55% wash). Both were appended to every paired light-theme rule group (a python pass extended 21 selector groups; 6 comment-preceded groups needed manual repair — check brace balance after any future regex pass!). Both are in slateThemes AND photoSlateThemes in theme-preview.js.
- HEADER (all themes, mockup-exact): inline diamond SVG + serif "PeerSlate" wordmark (no more logo img), nav = Home / Example Slate / Slate Board / Interview Me / About (active = accent + underline via aria-current), THEME DROPDOWN button (panel of 9 named swatches — swatch gradients moved to --swatch consumed by ::before; panel needs .theme-menu__panel[hidden]{display:none} because its display:grid beats the UA hidden rule), navy Sign In (--ink-btn-* vars; freed from slab/white overrides).
- PAGES REBUILT EXACTLY PER MOCKUP (ps-*/bd-* class families, all theme-var driven):
  - HOME (peerslate.html): serif underlined hero, live-data profile preview card + dark "Ask this Slate" pill w/ 4 action chips, 4 feature cards, How It Works w/ dashed connectors, Slate Boards feed cards w/ colored flags, See A Real Example band. Footer privacy line added globally in base.html.
  - OVERVIEW (index.html): profile band (avatar / Open-to-conversations chip / serif name / blue serif title / meta row) + About Pete card (in profile_shell.html, shown on portfolio pages EXCEPT board+interview — mockups treat those as standalone), tabs = Overview/Evidence(→skills)/Projects/Slate Board/Resume/My Slate; page = 4 metric cards, DARK ASK BAR wired to the REAL AI search partial (chips prefill+submit; needed a photo-theme specificity override to keep the input transparent), Core Skills grid, 3 Experience role cards, 4-card Projects Snapshot, retained lifecycle figure ("How I Work" card), Let's-build-what's-next band. Old hero/metrics-strip/featured-projects sections are gone from Overview (metrics_strip partial still used by my_story).
  - SLATE BOARD: hero + Share/Add buttons, 4 stat cards w/ links, white-card My Whiteboard (4 columns, vertical rules, Caveat ink w/ ○ bullets — links to cards preserved — sticky notes), tracking cards w/ colored PROGRESS BARS (rings retired), 3-up Latest cards w/ flag chips, bottom row: Momentum SVG line chart / 6 clip-path SHIELD badges / Slate Assistant (live AI input + prefill chips).
  - INTERVIEW ME: pastel ? icon on the question card, Record Answer now blue primary, serif score.
- Scroll NOTE: preview screenshots sometimes reset scroll/capture as thumbnails — scrolling itself works (verify via scrollIntoView + getBoundingClientRect, not scrollTop reads; html has scroll-behavior smooth).
- style.css ?v=mockup-system-2, theme-preview.js ?v=slate-themes-7.

## Recent Session Updates (2026-07-06 night — INTERVIEW ME page, Slate Paper theme, My Slate rename, Mac, branch main)
- NEW PAGE: /petec/interview-me ("Interview Me" tab, last in the strip). Interview Practice Studio per Pete's mockup: hero + "Interview with confidence" card, 4 benefit cards, mode bar (STAR Stories / Behavioral / Leadership / Mixed pills + "Use My Slate ✓" chip + Show Supporting Evidence toggle), two-column studio (Current Question w/ prev-next/rephrase, Response draft w/ 1,200-char counter + Save Draft (localStorage) + RECORD ANSWER (SpeechRecognition voice input, disabled w/ tooltip where unsupported) + Get Feedback, Coach Feedback card w/ 0-100 SCORE RING + session average, Suggested Questions + Start Mock Interview), right rail (Evidence Suggestions from Pete's real metrics + STAR Framework Helper + "Qualification Match — In design" teaser for the future job-match meter). Below: TWO 60-question banks in the HTML (30 STAR "tell me about a time", 30 behavioral) — clicking any loads it into the studio. static/js/interview.js runs it all; AI calls hit the SAME /api/chat endpoint as the chatbot (prompt asks for coaching points then "Score: NN/100", parsed by regex). Verified end-to-end: real feedback + score render.
- RENAME: "My Story" is now "MY SLATE" everywhere visible (tab, page title, eyebrow). URLs unchanged (/my-story still works) so no links break.
- NEW 9TH THEME "Slate Paper" (data-theme="paper-slate"), from Pete's second mockup set: warm near-white paper, navy primary (#16305e buttons, #1e4fa3 accent), royal-blue links, and PLAYFAIR DISPLAY serif for h1/h2/h3 (loaded in base.html; UI text stays Inter). It's a slate-family theme WITHOUT the stone photo: added to slateThemes in theme-preview.js but NOT photoSlateThemes → gets platform layout + procedural paper grain, no rock slabs. Theme dot: cream→navy gradient. The Interview Me hero/question use Playfair on ALL themes (the page's identity).
- SLATE BOARD upgrades from the mockups: whiteboard ink bullets are now little ○ circles; each section got a colored STICKY NOTE (blue/green/violet/orange + emoji, Kalam font, rotated); goal cards moved --ring to the card and gained a status line ("On track · Target: Jan 2027" with a ring-colored glowing dot).
- Review-workflow fixes shipped: Light Slate theme-selector dot was still cream→gold (now cream→green); .goal-chip--personal/.entry-flag--private tiny text failed WCAG AA on light themes — now color-mix(cyan 55%, text 45%); light themes' --slab-hi/lo edge tints moved into the LATE override blocks (cascade gotcha — early copies were dead); removed a no-op --slate-grain (photo themes override the grain layer entirely, so setting it there does nothing).
- Studio cards are stone slabs on photo themes (.iv-card/.iv-benefit/.iv-confidence in the card slab list; full-width .iv-modebar uses the wide cover-crop recipe — remember the 820px tile seam rule).
- Cache-busters: style.css ?v=interview-paper-1, theme-preview.js ?v=slate-themes-6, interview.js ?v=interview-1.

## Recent Session Updates (2026-07-06 evening — MY SLATE BOARD page + home feed + Light Slate goes green, Mac, branch main)
- NEW PAGE: /petec/slate-board ("Slate Board" tab, last in the strip after Projects). Pete's vision: goals + visions (business AND personal), progress tracking, badges/awards, and entries that can be Draft / Private / Public — public ones go to the site and the platform home feed. MVP is a fully designed STATIC preview (all data is sample content in templates/slate_board.html; compose buttons are aria-disabled visuals). Route in app.py + '/slate-board' added to the canonical section_paths.
- Page order (Pete): The Whiteboard → What We're Tracking (4 goal cards) → Latest From the Board (entries) → Momentum (bar chart) → The Trophy Shelf (8 badges, 2 locked) at the bottom.
- THE WHITEBOARD: a real-looking board (silver frame, white surface, marker tray + eraser) with up to FOUR user-named sections (Pete's: Short Term / Long Term / Projects / Work). Entries render as colored marker INK in the Kalam font (legible print handwriting, NOT cursive — loaded via extra_head only on this page). EVERY ink line is a link to the card it created (goal cards/entries have ids; project lines link to /petec/work). Board content scrolls inside (max-height 470px desktop / 520 phone). Fixed light colors on all themes — it's a physical object on the slate. Ink animates in line-by-line (--i stagger), momentum bars grow up, cards lift on hover — all inside @media (prefers-reduced-motion: no-preference).
- Goal rings are now COLORFUL: each card sets --ring inline (violet/green/blue/amber) + --p percent. Cards tightened per Pete ("bubbles a little tighter").
- HOME FEED: peerslate.html has a "From the Slate Boards" section (after How It Works): fixed THREE feed cards + a "Visit Pete's Slate Board" link — deliberately NOT an infinite scroll (Pete: tasteful, no doom-scrolling).
- WIDE-SLAB GOTCHA: the slab-card recipe's 820px stone tile visibly seams/repeats on elements wider than ~820px. Wide board surfaces (.board-compose/.board-momentum/.board-entry) use a cover-crop override like .metrics-strip. Remember this for any future full-width slate card.
- LIGHT SLATE REFRESHED (Pete): whiter page (#f7f7f4), DEEP GREEN accent scheme (#2e7d52 family) replacing the old warm gold — buttons, tab underline, metric values, gilded keywords (polished green), and lifecycle arrows (per-theme recolor like blueprint-light's) all follow. Texture turned UP to balance the whiter page: --slate-grain 0.34 + stronger veins, set in the LATE light-slate block (cascade gotcha), thinner body wash + thinner slab tints so the stone reads through.
- style.css cache-buster now ?v=whiteboard-1.

## Recent Session Updates (2026-07-06 later — tabs reorder, gilded keywords, lifecycle pop, Mac, branch main)
- Profile tab strip is now Overview / My Story / Resume / Projects — the Skills tab LINK was removed per Pete (the /petec/skills page itself still exists and works, it's just not in the strip).
- GILDED KEYWORDS: the highlighted spans in the two hero headlines ("better outcomes" on Overview, "Behind the Work" on My Story) render as gradient "polished metal" lettering (background-clip: text) lifted by --gilt-shadow drop-shadows. Mixed from the theme accent, so gold themes gild and light-blue takes polished blue. text-shadow must stay none on these spans (gradient fill is transparent; a text-shadow smears through it).
- LIFECYCLE POP (Overview): center medallion enlarged to 224x140 with a deep --pop-shadow; all six stage cards ride --slab-card-shadow + --pop-shadow; stage icons cast small drop-shadows; medallion title takes the engraved type shadow; arrows thickened to 2.4 stroke with a stronger glow.
- 5→6 ARROW FIX: the left cards (Verify, Deploy — both 122px tall) left only a 33px slit so the connector arc was buried under both cards. Deploy moved top 103→92, Verify bottom 60→48 → 56px gap (matches right side), and ALL arrow paths in index.html were redrawn against measured card boxes (left cards y 92-214 / 270-392, right y 117-224 / 278-366). If card positions/sizes change, re-measure with offsetTop/offsetHeight and redraw the arcs.
- CSS CASCADE GOTCHA (caught by adversarial review; cost a real bug): per-theme variable overrides for shared slate vars CANNOT live in the early body[data-theme="light-*"] blocks (~line 3900-4070) — the body[data-slate-photo="on"] block later in the file re-declares them at equal specificity and wins. Light-theme overrides for --pop-shadow/--gilt-shadow (and the pre-existing --slab-*-shadow/--slate-type-shadow overrides) live in the LATE light-theme blocks near the end of the file (search "Bars: soft light shadows"). Comments were added at both sites.
- style.css cache-buster now ?v=lifecycle-pop-1.

## Recent Session Updates (2026-07-06 — mockup-matching cosmetic overhaul + mobile redesign, Mac, branch main)
- Pete added 4 reference mockups in static/Background/ (image=light-slate, image2=light-blue-slate, image3=gray-slate, image4=sage-slate). These are the DESIGN NORTH STAR for the four slate themes — consult them before any slate styling change.
- ONE-BIG-BACKGROUND RULE: each page reads as one continuous slate background. body[data-slate-photo="on"] .profile-header is now background: transparent (no tint, no veins, no border) so the Pete Carter band can never mismatch the page again. The platform header and tab strip are the darker "chrome" cuts of the SAME fixed photo (new per-theme --slate-tint-header + darker --slate-tint-tabs).
- NO GOLD BORDERS ON SLATE THEMES: gray/sage --border vars were gold-tinted — now neutral white/sage-ink hairlines. --slate-vein-soft now mixes from --text (was --accent). Also neutralized: profile-tabs bottom border, metric divider ::after (hidden inside .metrics-strip), .view-all-link (now a raised slate chip), skill-popover tail, value-chip icon rings, floating #chat-toggle (now solid satin gold, was gold-outlined ghost). Gold on HOVER/selected states was kept deliberately (interaction feedback).
- Impact metrics strip: raised slab (darker bar wash + --slab-bar-shadow) with 44px raised circular icon badges (neutral ring, lit top edge, --slab-chip-shadow), vertically centered; icons colored var(--text), values gold.
- Gold buttons: --slab-primary is now smooth satin (radial top highlight + light→deep gradient mixed from --button-primary-bg, NOT --accent — this also fixed light-blue-slate's broken blue→gold button gradient). No grain/vein on buttons.
- Typography: light themes get crisp type (--slate-type-shadow is just a hairline white letterpress; the old dark emboss looked dirty on light stone). Dark themes' type shadow softened to 2 layers. Slate headlines are weight 800 / letter-spacing ~-0.03em (was 900 / -0.05em, read compressed vs mockups).
- Light themes darken the silver PEERSLATE header wordmark via filter brightness(0.34) (it washed out on light stone).
- Buttons size to content: removed min-width:190px from .btn-primary/.btn-secondary (Pete: "buttons much longer than the actual text").
- Platform home hero logo now uses the TRANSPARENT peerslate-logo-header.png lockup (752x150, alpha) with letterform drop-shadows — the old peerslate-logo.png (opaque navy 1200x900 box) looked pasted on. peerslate-logo.png is still used for og:/twitter: meta images.
- MOBILE (<744px) REDESIGNED: platform header is 2 compact rows (brand left + theme dots/Sign In right, then a scrollable nav-link strip) instead of 4 stacked rows; "Pete's Slate" label hidden ≤620px (didn't fit beside the dots). Profile band: 64px avatar (56 ≤520), 1.7rem name, Download Resume + Ask Pete AI share ONE row (nowrap, 0.78rem). Tabs 46px; .btn drops to 42px/0.86rem on phones; hero CTAs auto-width on slate; metric badges 38px. All in the 780/620/520 media blocks at the END of style.css.
- style.css cache-buster now ?v=mockup-polish-1.
- Verified: all 4 slate themes at 1280 desktop (overview/resume/my-story/work/skills) + 375 phone (overview/my-story/resume/home), no console errors. NOT COMMITTED yet at time of writing.

## Recent Session Updates (2026-07-05/06 — PeerSlate redesign pulled, iPad-sizing attempt reverted, Mac, branch main)
- IMPORTANT: the Mac repo was 4 commits behind GitHub — a full "PeerSlate platform" redesign built elsewhere (tabbed profile shell, platform nav with Career Search/My Network/Explore Profiles/For Recruiters, 8 themes with gray-slate as the new default, slate photo backgrounds in static/images/slate-backgrounds/, mockups in static/images/background-templates/, app.py rewritten with flask-limiter + canonical-URL redirects). Pulled onto the Mac this session. Much of the theme/layout info in the sections below this one is now OUTDATED (blueprint-light is no longer the default, the old site-menu header is gone).
- .claude/launch.json gotcha: the pull brought the Windows python path (venv/Scripts/python.exe); switched back to venv/bin/python for Mac. This will flip-flop between machines until made smarter.
- Pete's ask: every screen from iPad Mini (744px) on up should show the desktop layout, not a stacked/blown-up mobile-style one.
- FIRST ATTEMPT (reverted — do not redo this): retargeted every tablet breakpoint in style.css/resume.css to max-width:743px (phone-only) and added a script to scale desktop browser windows down with CSS `zoom` between 744-1279px. This broke the profile header bar's background color: `.profile-header` (and `.profile-tabs`) use `background-attachment: fixed` so their stone-photo texture lines up seamlessly with the page behind them (see "Align profile band and tab strip stone crops on slate themes" in git log) — `zoom` moves an element visually but fixed-attachment backgrounds stay pinned to the true, un-zoomed browser viewport, so the header's texture no longer lined up with the page around it once zoomed (worse in Safari, which Macs default to). It ALSO broke `.platform-nav`: the nav's own 1180px breakpoint (nav links wrap to a second row once they don't fit) is load-bearing, not cosmetic — collapsing it to 743px too made the logo/nav-links/theme-dots/sign-in overlap at any width below ~1180px, since the header genuinely does not fit in one row below that. Lesson: don't assume every tablet breakpoint in a design is "mobile-style bloat" — check what each one actually does before retargeting it.
- FINAL STATE (2026-07-06): style.css and resume.css breakpoints are back to exactly what was pulled from GitHub (1180/1040/860/780/980/1240/900/1020/940px etc., all untouched) — confirmed identical via `git diff 2a39b72 -- static/css/style.css static/css/resume.css` (empty). Only base.html changed: the real-tablet part of the sizing script survives (swaps `<meta viewport>` to width=1280 on genuine touch devices ≥744px — native browser scaling, doesn't touch backgrounds, so it doesn't have the fixed-attachment bug). The desktop-browser-window zoom hack was removed entirely; resizing a desktop browser window into tablet widths now just uses the site's normal (original) responsive CSS.
- Practical implication: a real iPad Mini gets pixel-perfect desktop layout (via the viewport-meta trick). A desktop browser window manually narrowed to iPad-Mini width will NOT be pixel-identical to desktop — it'll gracefully reflow using the original tablet breakpoints (nav wraps to two rows around 1180px, hero/lifecycle stack around 1180px, full phone stacking still starts at 780px). This was a deliberate trade-off after the pixel-perfect approach proved incompatible with the slate-photo theme's fixed backgrounds.
- Verified in preview at 744px and 1000px (both screenshot cleanly: consistent header/page colors, no nav overlap, normal-sized buttons) and 1440px (DOM/bounding-box check only — the screenshot tool had an unrelated thumbnail-rendering glitch at that exact size that reproduced even on a fresh server with no code involved).

## Recent Session Updates (2026-07-05 — Blueprint Light theme, Mac, branch main)
- New "Blueprint Light" theme added and made the SITE-WIDE DEFAULT (base.html body data-theme + theme-preview.js defaultTheme both say blueprint-light; meta theme-color now #f4f9fc). Returning visitors with a saved theme in localStorage (key peerslateTheme) keep their old choice — clear localStorage to see the new default.
- Palette (from Pete's codex instructions file): pale blueprint-blue page (#f4f9fc) with a faint 48px grid, navy text (#08204a), teal/cyan primary (#0796b8 / #0ea5c6) mapped onto the existing --color-gold variable slots, blue secondary (#1f6feb), soft blue-gray borders (#cfe0eb), navy-tinted shadows.
- Amber (#f59e0b) is reserved for money metrics only: metrics_strip.html now wraps $36M+/$900M/$19.2M in <strong class="metric-money"> (both scrolling sets); the class is styled only under blueprint-light.
- Implementation: variable block sits with the other theme presets near the top of style.css; structural overrides live at the end of style.css's LIGHT THEME COMPATIBILITY section (mirrors the slate-light pattern); resume-page-specific overrides appended to resume.css (cache-buster bumped to ?v=blueprint-light-1); theme button added to the site-menu dropdown (5 themes now); theme-preview.js cache-buster bumped to ?v=theme-preview-2.
- Hardcoded-dark surfaces that needed explicit light overrides: .hero/.my-story-page/.work-page/.resume-page circuit banners, .work-hero-panel, .story-journey__layout, .fun-fact-card, .drive-card, .timeline-item__icon (+ its --light icon variant), lifecycle .cycle-arc--gold arrows + #arrow-gold SVG marker (teal in light mode), #chat-input, #chat-send, .resume-ai-panel/.resume-ai-suggestion/.resume-skill-button/.resume-skill-popover.
- Verified in preview on home, Projects, My Story, Resume, Contact + chat widget; no console errors. Left dark on purpose: .mock-sidebar and .mock-window--dark (app-screenshot mockups inside project cards).
- SLATE LIGHT THEME REMOVED (same session): dropdown button deleted from base.html, variable block + entire compatibility section deleted from style.css (~210 lines). theme-preview.js now validates the saved localStorage theme against the rendered theme buttons and falls back to the default if the saved name no longer exists (protects visitors who had slate-light saved); cache-buster bumped to ?v=theme-preview-3. Site now has 4 themes: command-gold, modern-blue, blueprint-light (default), secure-green.
- NOTE: Flask runs with debug off unless FLASK_DEBUG=true, so Jinja caches templates — template edits need a server restart to show up in the preview.
- MOBILE TYPE SCALE (same session): Pete flagged that the homepage and resume page looked "blown up" on phones vs My Story. My Story's phone hero (24px h1 at 375px wide) is now the reference scale: homepage .hero-copy h1 phone clamps reduced (780px block: clamp(1.6rem,5vw,2.3rem); 620px block: clamp(1.5rem,6.4vw,2.05rem)); .projects-copy h2 phone clamp reduced to clamp(1.55rem,6vw,1.9rem) — note the 620px block has TWO .projects-copy h2 rules, the later one wins; hero Interactive Resume button stays auto-width on phones (all other .btn stay full-width); resume-hero h1 phone clamp reduced from 12vw (45px!) to clamp(1.6rem,6.4vw,2.2rem) and .resume-hero__name to 1.02rem (resume.css cache-buster now ?v=mobile-scale-1). Rule of thumb going forward: phone headline sizes should match My Story's scale (~24px h1, ~24px section h2 at 375px).

## Recent Session Updates (2026-07-04 — interactive resume redesign, Mac, branch main)
- /petec/resume page rebuilt to match Pete's mockup: hero is now name headline (left) + "Ask Pete's AI Assistant" panel (right) with the shared search partial and 4 suggested-question chips (2x2) from resume_data.json suggested_ai_questions.
- Removed from the resume page: Recruiter Quick View track filtering (and all its JS), the full-width AI bar below the hero, the Skills to Evidence category filter buttons and skill front summaries, the Selected Engineering Impact case-study section, and the Download/Print/LinkedIn bar. resume_data.json itself was NOT changed — unused data (recruiter_tracks, case_studies, metrics 5-8) is still there if needed later.
- Impact strip now shows only the first 4 metrics from resume_data.json ($36M+, 16 to 0, 70%, 52%); cards still click through to the connected role evidence. Metric grid is 4-across / 2x2 at every width per the groups-of-4 rule.
- Skills: all 21 public skills as compact name-only cards, 7 across on desktop (3 rows), evidence popovers unchanged. Pete wants to fine-tune this grid later.
- resume.js rewritten slim (popovers, role selection, metric jumps, ask-AI buttons, suggestion chips). Cache-buster bumped to ?v=resume-redesign-5 on resume.css/resume.js.
- Career Experience and Education sections untouched.
- OBSERVED during testing: the chatbot answered a resume question with "two decades of experience," which does not match the career history (EE degree 2020). Knowledge-base/prompt quality issue to review.

## Recent Session Updates (2026-07-02 — header menu, Fun Facts, card polish; branch feature/my-story-page)
- Pete is now working on a Windows 11 machine (repo at C:\Users\peter\Documents\portfolio). venv activation is `venv\Scripts\activate`; .claude/launch.json points at venv/Scripts/python.exe. app.py reads the PORT env var (falls back to 5000).
- Site is planned to become a multi-tenant business (other people get their own portfolio sites). First step: top-left dropdown menu in the header (currently just "Homepage"); brand + nav links shifted right to make room.
- Header: LinkedIn icon now uses official brand colors (#0A66C2 + white); "Case Studies" nav renamed to "Projects" (still /work); Ask Pete AI buttons restyled — squared corners, gold fill/outline, uppercase.
- Homepage: "View Case Studies" hero button removed; "Interactive Resume" is now the single gold primary CTA (still "#" placeholder).
- Metrics strip is now a framed, rounded strip that stops at the content rail (no more edge-to-edge band on big screens); scrolling cards cut off cleanly at the frame ends.
- My Story: collage enlarged (up to 470px wide); hero text column stretches so the banner bottom-aligns with the collage (dead gap fixed); Let's Connect card removed; "Life Beyond the Work" expanded to 8 interests in a 4x2 grid; new FUN FACTS section shows 3 random facts per page load (pool lives in a script at the bottom of my_story.html) with a "Show Me More" shuffle button.
- PETS: Pete has two DOGS, Blazer and Falcon. The old "proud cat dad to Nala and Mochi" line was wrong and has been removed.
- Layout rule from Pete: any group of 4 items must display as 4-across or 2x2 at every screen width — never 3+1. Value chips, timeline, drive cards, and the interest grid all follow this now.
- Card hover rule from Pete: every card-style surface site-wide gets the project-card "pop out" hover (translateY(-4px) + border + shadow). Shared CSS rule covers .drive-card, .story-card, .fun-fact-card.
- KNOWN ISSUE to fix: homepage hero eyebrow still says "PhD Student" — violates the PhD wording rule below (admitted, starts January 2027). Ask Pete for preferred wording.

## Earlier Session Updates (2026-07-01 — My Story build, branch feature/my-story-page)
- New /my-story page (templates/my_story.html + route in app.py): hero with circular portrait placeholder, CURRENT FOCUS banner, journey timeline (2020 EE degree, 2021–2024 DoD/USAF, 2024–2025 L3Harris, 2025–Present Northrop Grumman — dates verified against docs/knowledge/career_history.md), values cards, three info cards, shared metrics strip.
- Header updated: "SYSTEMS ENGINEER" typo fixed; nav renamed to Case Studies / Expertise / My Story; Hobbies and About removed from nav (pages still exist at /hobbies and /about); LinkedIn icon button added (www.linkedin.com/in/pete-carter19); active-page gold underline added via aria-current="page"; phone nav links restored (were hidden under 620px).
- Metrics strip extracted into templates/partials/metrics_strip.html, included by both index.html and my_story.html, and made 15% thinner (66px min-height).
- Homepage hero headline changed to "Engineering better outcomes through technology, leadership, and systems thinking."; "Download Résumé" renamed to "Interactive Resume"; lifecycle box labels updated (Define / Requirements / Architecture / Implement & Integrate / Verify & Validate / Deploy & Sustain); blue inner arrows removed.
- Outstanding placeholders: real headshot for the My Story portrait; Résumé links still point to "#"; "View Full Timeline" temporarily links to /work.
- PhD wording rule: Pete is ADMITTED to the University of South Alabama Systems Engineering Ph.D. program with an expected January 2027 start — never describe him as a current PhD student before then.

## Earlier Session Updates
- Top navigation now stays sticky while scrolling on desktop/tablet screens, but scrolls away normally on phone screens.
- Chatbot suggestion buttons now disappear after the first submitted question.
- Chatbot prompt was improved for grammar, concise answers, plain text, short paragraphs, and stronger professional tone.
- Chatbot output cleanup was added on the server side to remove Markdown artifacts before responses reach the browser.
- Chatbot context selection was improved so Claude receives focused knowledge files based on the visitor's question instead of the entire knowledge base every time.

## Notes
- Pete is a complete beginner on Mac, limited PC experience too
- Prefers step-by-step, detailed explanations — no assumptions
- Using Cowork (Claude desktop app) to get help
- The venv folder must be activated before running Flask: `source venv/bin/activate`
- When code changes are made, explain the filename, actual line numbers, what the code was before, what it is now, and why the change works
- Add helpful comments directly in edited files when the change teaches an important concept or prevents future confusion
- Before editing files Pete may have open in VS Code, remind him to press Cmd+S
